{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": [
      "node"
    ]
  },
  "files": [
    "src/main.ts",
    "src/polyfills.ts" // Corrigido para incluir polyfills.ts
  ],
  "include": [
    "src/**/*.d.ts",
    "src/**/*.ts" // Inclui todos os arquivos TypeScript no diretório src
  ]
}
